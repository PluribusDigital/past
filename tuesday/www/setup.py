from setuptools import setup

setup(name='api',
      version='0.1',
      description='Prior Art Search Tool',
      author='Jeff Farley',
      packages=['api'],
      install_requires=[],
      test_suite='tests',
      zip_safe=False)