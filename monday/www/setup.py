from setuptools import setup

setup(name='nlp',
      version='0.1',
      description='Prior Art Search Tool - Natural Langugage Processor',
      author='Jeff Farley',
      packages=['nlp'],
      install_requires=[],
      test_suite='tests',
      zip_safe=False)